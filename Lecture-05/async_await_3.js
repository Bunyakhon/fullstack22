// 1. ฟังก์ชันส่งค่าตัวละคร (รอ 0.2 วินาที)
function who() {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("🤡");
    }, 200);
  });
}

// 2. ฟังก์ชันส่งคำกริยา (รอ 0.3 วินาที)
function what() {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("lurks");
    }, 300);
  });
}

// 3. ฟังก์ชันส่งสถานที่ (รอ 0.5 วินาที)
function where() {
  return new Promise((resolve) => {
    setTimeout(() => {
      resolve("in the shadows");
    }, 500);
  });
}

// ฟังก์ชันหลักที่รวบรวมคำตอบ
async function msg() {
  // ใช้ await เพื่อรอให้แต่ละตัวทำงานเสร็จตามลำดับ
  const a = await who();
  const b = await what();
  const c = await where();

  console.log(`${a} ${b} ${c}`);
}

// --- ส่วนของการรันโค้ด ---
console.log('We are looking for:');

msg(); // จะใช้เวลาประมาณ 1 วินาที (200 + 300 + 500 ms) ถึงจะแสดงผล

console.log('Hello');